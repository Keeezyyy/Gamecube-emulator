#pragma once

extern void emit_addi(u32 **start,
                      u32 **end); // imm val is on the stack
extern void emit_ori(u32 **start,
                     u32 **end); // imm val is on the stack
